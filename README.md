# Proyecto ECO727_P1

Aplicación web estática y backend básico con Django + React (Vite).

## Estructura
- **backend/** → Proyecto Django (API o base de datos)
- **frontend/** → Página estática React (sitio visible)
- **.venv/** → Entorno virtual (no se sube)

## Despliegue
El frontend se encuentra desplegado en Netlify:
👉 [https://tusitio.netlify.app](https://tusitio.netlify.app)

Backend pendiente de despliegue (Render o Railway)
